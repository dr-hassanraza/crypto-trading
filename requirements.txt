# Streamlit Dependencies (Core - Must be first)
streamlit>=1.28.1
plotly>=5.17.0
pandas>=2.0.0
numpy>=1.24.0

# Optional dependencies for enhanced features
# (These will be imported with try/except in the app)
requests>=2.31.0
python-dotenv>=1.0.0

# Web framework (optional - only for local FastAPI server)
# fastapi>=0.104.1
# uvicorn[standard]>=0.24.0

# Data Analysis (lightweight versions)
scipy>=1.11.0

# Visualization
# dash>=2.14.0
# dash-bootstrap-components>=1.5.0

# Note: Heavy ML libraries removed for Streamlit Cloud compatibility
# The app includes mock data generators so it works without these dependencies:
# - tensorflow, torch, transformers (ML models)
# - scikit-learn, xgboost, lightgbm (ML algorithms) 
# - ta (technical analysis)
# - ccxt, python-binance (crypto APIs)
# - tweepy, newsapi-python (social media)
# - web3 (blockchain)
# - stable-baselines3 (RL)
# - hmmlearn, statsmodels (advanced analytics)

# These can be added back if needed for specific features:
# scikit-learn>=1.3.0
# yfinance>=0.2.22
# textblob>=0.17.1